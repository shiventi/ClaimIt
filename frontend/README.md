# ClaimIt Frontend

React app for the ClaimIt dashboard.

## Scripts

- `npm start` - Start developement server
- `npm run build` - Build for production
- `npm test` - Run tests

## Environment

Set `REACT_APP_API_BASE_URL` in root .env file.
